# Secrets — never commit
.env

# Xbox Live / MSA token cache written by prismarine-auth via bedrockx's
# createClient `profilesFolder` option — holds live, reusable Xbox Live tokens
# for the bot's Microsoft account. NEVER commit.
.secrets/

# --- Node ---
node_modules/
npm-debug.log*
dist/

# --- OS / editor ---
.DS_Store
Thumbs.db
.vscode/
.idea/

# --- Claude Code local settings ---
.claude/

# --- Codex / other-agent local artifacts ---
.agents/
.codex/
# Launcher scratch (ephemeral images/logs written by the tray launcher)
.launcher-tmp/
